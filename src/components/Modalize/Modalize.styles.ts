import { StyleSheet } from 'react-native'

export const style = StyleSheet.create({
  motiview: {
    paddingBottom: 40,
    backgroundColor: '#000'
  },
  modalize: {
    marginBottom: -50,
    width: '100%',
    flex: 1
  }
})
